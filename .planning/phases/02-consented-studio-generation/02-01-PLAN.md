---
phase: 02-consented-studio-generation
plan: 01
type: execute
wave: 1
depends_on: [01-03]
files_modified:
  - apps/web/components/studio-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/studio-generation.spec.ts
  - services/api/app/schemas/generation.py
  - services/api/app/services/stub_generation.py
  - services/api/tests/test_generate_stub.py
autonomous: true
requirements: [GOV-04, STUD-03, STUD-04, STUD-05]
must_haves:
  truths:
    - "D-05/D-07/D-13/D-14/D-15/D-16 and STUD-03/STUD-04: the root studio accepts text and the fixed Measured/Cutting/Grandiose preset set, keeps the voice copy prototype-labeled, and avoids sliders, protected-character language, or other tone knobs."
    - "D-09/D-10/D-12 and STUD-05: submitting generation produces a visible queued/running job state with loading and error feedback instead of a blocking request."
    - "GOV-04 and the Phase 1 rights gate stay backend-owned: consent/license metadata is preserved in the server contract and unapproved voices still fail through `ensure_voice_allowed` before any generation work."
    - "README drift stays a follow-up only; no task in this plan reuses the stale README stack as a source of product or infrastructure truth."
  artifacts:
    - "apps/web/components/studio-shell.tsx"
    - "apps/web/app/globals.css"
    - "apps/web/tests/studio-generation.spec.ts"
    - "services/api/app/schemas/generation.py"
    - "services/api/app/services/stub_generation.py"
    - "services/api/tests/test_generate_stub.py"
    - "GenerationRequest.text"
    - "GenerationRequest.tone_preset"
    - "GenerationTonePreset"
    - "GenerationJobStatus"
    - "GenerationResult.job_id"
    - "GenerationResult.status"
    - "GenerationResult.text"
    - "GenerationResult.tone_preset"
    - "build_stub_generation_result"
  key_links:
    - "StudioShell text input -> generation request schema -> rights gate -> queued job card."
    - "Tone preset chips -> `tone_preset` payload -> prototype baseline response metadata."
    - "Browser loading/error state -> stubbed job result -> exact blocked-rights message."
---

<objective>
**As a** no-login internal studio user, **I want to** enter text, choose a tone preset, generate speech through a real provider, and play the resulting audio, **so that** I can audition the consented theatrical voice loop end to end.

Purpose: Deliver the first user-facing generation slice that turns the Phase 1 shell into a job-shaped studio input flow.
Output: A text-and-tone generation form, a job-oriented stub response contract, and updated browser/API regression coverage.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consented-studio-generation/02-CONTEXT.md
@.planning/phases/02-consented-studio-generation/02-RESEARCH.md
@.planning/phases/02-consented-studio-generation/02-VALIDATION.md
@.planning/phases/02-consented-studio-generation/02-PATTERNS.md
@.planning/phases/01-no-login-vertical-skeleton/01-03-SUMMARY.md
@apps/web/components/studio-shell.tsx
@services/api/app/schemas/generation.py
@services/api/app/services/stub_generation.py
@services/api/tests/test_generate_stub.py
@apps/web/tests/studio-generation.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the studio browser spec around text, preset, and queued-job state</name>
  <files>apps/web/tests/studio-generation.spec.ts</files>
  <read_first>
    apps/web/tests/studio-generation.spec.ts
    apps/web/components/studio-shell.tsx
    services/api/app/schemas/generation.py
    services/api/app/services/stub_generation.py
    services/api/tests/test_generate_stub.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-VALIDATION.md
    .planning/phases/01-no-login-vertical-skeleton/01-03-SUMMARY.md
  </read_first>
  <behavior>
    - The happy path enters text, selects Measured/Cutting/Grandiose, submits generation, and expects a queued or running job badge.
    - The blocked-voice path still shows the exact rights message from Phase 1.
    - The slice still has no playback, microphone, upload, or live-conversation controls.
  </behavior>
  <action>
    Rewrite `apps/web/tests/studio-generation.spec.ts` so the browser contract now exercises the job-shaped text-and-tone flow instead of the Phase 1 metadata-only card. Keep the exact blocked-rights assertion, keep the root studio route, and make the spec fail against the current stub until the API and UI tasks land.
  </action>
  <acceptance_criteria>
    The spec expresses the new text + tone preset contract, expects job-state feedback, and is red against the current metadata-only implementation.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec playwright test tests/studio-generation.spec.ts</automated>
  </verify>
  <done>
    The browser test is now a failing contract for the Phase 2 text-and-tone slice.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the generation contract and stub response to carry job metadata</name>
  <files>services/api/app/schemas/generation.py, services/api/app/services/stub_generation.py, services/api/tests/test_generate_stub.py</files>
  <read_first>
    services/api/app/schemas/generation.py
    services/api/app/services/stub_generation.py
    services/api/tests/test_generate_stub.py
    services/api/tests/test_rights_gate.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-VALIDATION.md
  </read_first>
  <behavior>
    - `GenerationRequest` accepts `text` and one of the fixed tone presets.
    - The response carries `job_id`, `status`, `text`, `tone_preset`, rights metadata, provider trace, and timing.
    - The blocked rights response stays exact and still runs before any generation work.
  </behavior>
  <action>
    Extend `services/api/app/schemas/generation.py` with a fixed `GenerationTonePreset` enum, a job-status model that can represent the queued/running/succeeded/failed lifecycle, and a job-shaped `GenerationResult` that includes the text, tone preset, and job identifier. Update `services/api/app/services/stub_generation.py` so the Phase 1 stub becomes a queued prototype job that is clearly labeled as an early baseline per D-02/D-07 while preserving the compact rights-check, provider-trace, and timing shape. Update `services/api/tests/test_generate_stub.py` so it asserts the new request fields and job-shaped response instead of the old metadata-only card.
  </action>
  <acceptance_criteria>
    `POST /generate` accepts text plus a fixed tone preset, returns a queued prototype job response, and still returns the exact 403 rights message for unapproved voices.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/api/tests/test_generate_stub.py services/api/tests/test_rights_gate.py -q</automated>
  </verify>
  <done>
    The backend contract now matches the Phase 2 job-shaped studio seam while keeping the server rights gate intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild the studio shell for text entry and tone presets</name>
  <files>apps/web/components/studio-shell.tsx, apps/web/app/globals.css</files>
  <read_first>
    apps/web/components/studio-shell.tsx
    apps/web/app/globals.css
    apps/web/tests/studio-generation.spec.ts
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
  </read_first>
  <behavior>
    - The root studio renders a text area, the three fixed preset options, and a submit button.
    - Loading and error text remain visible and the voice selector / rights badge stay in place.
    - No playback controls are added yet.
  </behavior>
  <action>
    Replace the Phase 1 stub trigger in `StudioShell` with the minimal Phase 2 generation form: keep the bundled voice selector and rights badge, add a text field and the Measured/Cutting/Grandiose preset chips with compact one-line descriptions, submit the new job-shaped request, and surface the queued/running/error state copy from D-13/D-15/D-16. Keep D-05 in force by not adding reference-audio upload, recording, or consent intake UI, and update `apps/web/app/globals.css` so the new controls fit the existing theatrical shell without adding sliders or playback UI.
  </action>
  <acceptance_criteria>
    A user can enter text, choose one of the three presets, submit the form, and see the job-state card at the root route with the current voice and rights badge still visible.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec playwright test tests/studio-generation.spec.ts</automated>
  </verify>
  <done>
    The studio now exposes the first user-facing generation slice: text, tone, submit, and job-state feedback.
  </done>
</task>

</tasks>

## Artifacts this phase produces
- `GenerationTonePreset`
- `GenerationJobStatus`
- `GenerationRequest.text`
- `GenerationRequest.tone_preset`
- `GenerationResult.job_id`
- `GenerationResult.status`
- `GenerationResult.text`
- `GenerationResult.tone_preset`
- `build_stub_generation_result`
- `apps/web/components/studio-shell.tsx`
- `apps/web/app/globals.css`
- `apps/web/tests/studio-generation.spec.ts`
- `services/api/app/schemas/generation.py`
- `services/api/app/services/stub_generation.py`
- `services/api/tests/test_generate_stub.py`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/generate` | Untrusted text and preset input cross into the server contract. |
| Client UI → rights gate | The browser cannot decide generation eligibility; the server must enforce it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | `GenerationRequest` / studio form | medium | mitigate | Use strict Pydantic validation, fixed preset values, and `extra="forbid"` so freeform prompt drift cannot enter the request. |
| T-02-01-02 | Elevation of Privilege | `ensure_voice_allowed` / `/generate` | high | mitigate | Keep the server-owned rights gate ahead of any generation work and preserve the exact blocked-message path. |
| T-02-01-SC | Tampering | phase package/runtime setup | low | accept | This slice introduces no new model/runtime install path; the suspicious provider work is deferred to the worker plan and gated there. |
</threat_model>

<verification>
[Overall phase checks]
- After Task 3, the browser spec and the backend stub/rights tests should be green together.
- The phase slice is complete when the root studio accepts text and tone presets and the server returns the new queued job-shaped stub contract.
</verification>

<success_criteria>
- The root studio accepts text input and one of the three fixed tone presets.
- Submitting generation shows loading and a job-oriented queued state.
- The exact blocked-rights message still appears for unapproved voice profiles.
- The phase remains free of playback, sliders, microphone, upload, live-conversation, and durable-library UI.
</success_criteria>

<output>
Create `.planning/phases/02-consented-studio-generation/02-01-SUMMARY.md` when done.
</output>
