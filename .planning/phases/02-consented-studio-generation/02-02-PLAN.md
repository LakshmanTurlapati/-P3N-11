---
phase: 02-consented-studio-generation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - services/speech-worker/providers/cosyvoice_provider.py
  - services/speech-worker/audio/normalization.py
  - services/speech-worker/providers/__init__.py
  - services/speech-worker/tests/test_cosyvoice_provider.py
  - services/speech-worker/tests/test_audio_normalization.py
autonomous: false
requirements: [PIPE-02, PIPE-04]
must_haves:
  truths:
    - "D-01/D-03/D-04/D-06/D-21 and PIPE-02: the approved baseline provider can generate a playable, tone-steered prototype audition clip through the provider contract."
    - "D-02/D-08 and PIPE-04: generated clips are complete, prototype-labeled outputs that are normalized before storage or playback; no streaming or partial audio is introduced here."
    - "The worker slice stays text-to-speech focused and does not add reference-audio intake UI; consent and license notes remain backend-owned."
  artifacts:
    - "services/speech-worker/providers/cosyvoice_provider.py"
    - "services/speech-worker/audio/normalization.py"
    - "services/speech-worker/providers/__init__.py"
    - "services/speech-worker/tests/test_cosyvoice_provider.py"
    - "services/speech-worker/tests/test_audio_normalization.py"
    - "CosyVoiceTTSProvider"
    - "normalize_audio"
  key_links:
    - "Human checkpoint approval -> approved CosyVoice baseline -> provider implementation."
    - "CosyVoiceTTSProvider.synthesize -> normalize_audio -> `SpeechArtifact` -> later object store/playback."
    - "Tone steering via the existing `tone` argument -> prototype audition audio."
---

<objective>
**As a** no-login internal studio user, **I want to** have a real server-side TTS baseline that can produce a playable clip, **so that** the later backend and browser slices can surface actual audio.

Purpose: Lock the first real TTS baseline behind the existing provider contract so the later job/storage slice can produce actual playable clips.
Output: A verified CosyVoice-backed worker adapter and an FFmpeg-backed normalization helper.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consented-studio-generation/02-CONTEXT.md
@.planning/phases/02-consented-studio-generation/02-RESEARCH.md
@.planning/phases/02-consented-studio-generation/02-VALIDATION.md
@.planning/phases/02-consented-studio-generation/02-PATTERNS.md
@.planning/phases/01-no-login-vertical-skeleton/01-03-SUMMARY.md
@services/speech-worker/providers/contracts.py
@services/speech-worker/tests/test_provider_contracts.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the baseline CosyVoice checkpoint and install path</name>
  <files>services/speech-worker/providers/cosyvoice_provider.py, services/speech-worker/audio/normalization.py, services/speech-worker/providers/__init__.py, services/speech-worker/tests/test_cosyvoice_provider.py, services/speech-worker/tests/test_audio_normalization.py</files>
  <read_first>
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-VALIDATION.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
  </read_first>
  <decision>Confirm the real provider baseline and the checkpoint/install path that the worker slice will use.</decision>
  <context>The research recommends the official CosyVoice repo checkout as the baseline real provider, with the 0.5B line preferred and 300M as a fallback. The package-legitimacy audit flagged the runtime and provider names as suspicious enough to require explicit human approval before any install or checkpoint download.</context>
  <options>
    <option id="cosyvoice-0_5b">
      <name>CosyVoice 0.5B baseline (Recommended)</name>
      <pros>Matches the research recommendation and gives the strongest first real-audio baseline for the prototype audition.</pros>
      <cons>Heavier setup than the smaller fallback.</cons>
    </option>
    <option id="cosyvoice-300m">
      <name>CosyVoice 300M fallback</name>
      <pros>Lighter setup if the recommended checkpoint cannot be approved or fetched.</pros>
      <cons>Lower ceiling for the prototype audition.</cons>
    </option>
  </options>
  <action>
    Review the approved CosyVoice baseline, confirm the checkpoint/install path, and block any later worker implementation until the approval is explicit. This gate exists because D-01/D-03/D-04/D-06/D-21 require a real provider baseline but the current install path is still under human review.
  </action>
  <acceptance_criteria>
    The human has selected a baseline and approved the exact checkpoint/install path that the later worker task will use.
  </acceptance_criteria>
  <verify>
    <human-check>Confirm the selected CosyVoice baseline and the approved checkpoint/install path before any worker code or runtime install work begins.</human-check>
  </verify>
  <done>
    The approved baseline and install path are explicit, and the later worker implementation can proceed without guessing.
  </done>
  <resume-signal>Select the approved option and state the approved install/download path.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing worker tests for the real provider and normalization helper</name>
  <files>services/speech-worker/tests/test_cosyvoice_provider.py, services/speech-worker/tests/test_audio_normalization.py</files>
  <read_first>
    services/speech-worker/tests/test_provider_contracts.py
    services/speech-worker/providers/contracts.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-VALIDATION.md
  </read_first>
  <behavior>
    - `CosyVoiceTTSProvider.synthesize(text, voice_id, tone=None)` returns a real `SpeechArtifact` with a playable WAV payload.
    - `normalize_audio` produces the accepted normalized clip shape before storage or playback.
    - The tests pin the prototype audition behavior, not final Vesper polish.
  </behavior>
  <action>
    Create `services/speech-worker/tests/test_cosyvoice_provider.py` and `services/speech-worker/tests/test_audio_normalization.py` as the failing contracts for the approved CosyVoice baseline. Assert that the provider remains behind `TTSProvider`, carries the tone argument through to the artifact, and feeds a normalization helper that yields the accepted playback format. Keep the assertions focused on the prototype audition shape from D-02/D-07 and the complete-clip requirement from D-08.
  </action>
  <acceptance_criteria>
    The new worker tests describe a real playable-audio baseline and an FFmpeg-backed normalization contract, and they fail until the implementation task lands.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/speech-worker/tests/test_cosyvoice_provider.py services/speech-worker/tests/test_audio_normalization.py -q</automated>
  </verify>
  <done>
    The worker slice now has executable contracts for the approved real provider and the normalization helper.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the CosyVoice provider adapter and audio normalization helper</name>
  <files>services/speech-worker/providers/cosyvoice_provider.py, services/speech-worker/audio/normalization.py, services/speech-worker/providers/__init__.py</files>
  <read_first>
    services/speech-worker/providers/contracts.py
    services/speech-worker/tests/test_cosyvoice_provider.py
    services/speech-worker/tests/test_audio_normalization.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
  </read_first>
  <behavior>
    - The provider returns complete clips through the existing `TTSProvider` contract.
    - The normalization helper emits the accepted playback shape for later storage and browser playback.
    - The provider is exported from the worker package surface for the API slice to import without knowing the model internals.
  </behavior>
  <action>
    Implement `CosyVoiceTTSProvider` in `services/speech-worker/providers/cosyvoice_provider.py` with `provider_name = "cosyvoice"` and a `synthesize(text, voice_id, tone=None)` method that uses the approved CosyVoice repo checkout as the safe real baseline per D-01/D-04/D-06/D-21. Add `normalize_audio` in `services/speech-worker/audio/normalization.py` to convert the provider output into the accepted clip format using FFmpeg-backed normalization. Update `services/speech-worker/providers/__init__.py` so the adapter is importable from the worker surface. Do not introduce the removed OpenVoice package path.
  </action>
  <acceptance_criteria>
    A configured real CosyVoice baseline can produce a playable clip through the worker contract, and the clip is normalized before it leaves the worker helper.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/speech-worker/tests/test_cosyvoice_provider.py services/speech-worker/tests/test_audio_normalization.py -q</automated>
  </verify>
  <done>
    The first real provider baseline is ready for the job/store slice to consume.
  </done>
</task>

</tasks>

## Artifacts this phase produces
- `CosyVoiceTTSProvider`
- `normalize_audio`
- `services/speech-worker/providers/cosyvoice_provider.py`
- `services/speech-worker/audio/normalization.py`
- `services/speech-worker/providers/__init__.py`
- `services/speech-worker/tests/test_cosyvoice_provider.py`
- `services/speech-worker/tests/test_audio_normalization.py`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human approval → worker runtime | The approved baseline and checkpoint choice must be explicit before any install or download work begins. |
| Worker code → model artifacts | The provider adapter must not trust unreviewed checkpoint or runtime inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | `CosyVoiceTTSProvider` / checkpoint path | high | mitigate | Use the approved repo checkout and human-verified checkpoint before any synthesis work starts. |
| T-02-02-02 | Information Disclosure | `normalize_audio` / clip output | medium | mitigate | Normalize into the accepted clip shape and keep the worker from exposing raw checkpoint or filesystem details. |
| T-02-02-SC | Tampering | provider/runtime installs and checkpoint downloads | high | mitigate | Block all suspicious provider/runtime install or checkpoint steps until the human checkpoint approves the exact baseline. |
</threat_model>

<verification>
[Overall phase checks]
- The worker tests pass after the approved baseline is implemented.
- A real CosyVoice-backed clip can be produced through the worker contract before the phase is considered complete.
</verification>

<success_criteria>
- The worker exposes one real CosyVoice baseline behind `TTSProvider`.
- The worker returns complete clips, not streams or partial audio.
- Audio is normalized through the worker helper before it is handed to later storage or playback code.
- The selected baseline and install path were explicitly human-approved before implementation began.
</success_criteria>

<output>
Create `.planning/phases/02-consented-studio-generation/02-02-SUMMARY.md` when done.
</output>
