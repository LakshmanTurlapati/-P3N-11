---
phase: 02-consented-studio-generation
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - services/api/app/services/generation_runtime.py
  - services/api/app/routes/generate.py
  - services/api/tests/conftest.py
  - services/api/tests/test_generation_runtime.py
autonomous: true
gap_closure: true
requirements: [GOV-04, STUD-05, PIPE-02, PIPE-03, PIPE-04]
requirements_addressed: [GOV-04, STUD-05, PIPE-02, PIPE-03, PIPE-04]
must_haves:
  truths:
    - "D-01/D-02/D-04/D-06/D-07/D-21 and PIPE-02: queued generation jobs hand off to the safe real-audio CosyVoice baseline through the provider interface, and the runtime keeps the output framed as a prototype audition rather than final Vesper quality."
    - "D-09/D-10/D-12/D-19 and PIPE-03/PIPE-04: `POST /generate` stays non-blocking, the background task advances the job through `running` to `succeeded` or `failed`, and successful jobs write normalized audio plus a controlled playback URL."
    - "GOV-04 and D-03: the rights gate still runs before any synthesis work, and the API test suite can exercise the new runtime directly with fakes while route-based tests remain deterministic."
  artifacts:
    - "services/api/app/services/generation_runtime.py"
    - "services/api/app/routes/generate.py"
    - "services/api/tests/conftest.py"
    - "services/api/tests/test_generation_runtime.py"
    - "process_generation_job"
    - "get_generation_tts_provider"
    - "disable_background_generation"
    - "BackgroundTasks handoff from POST /generate"
    - "mark_running"
    - "mark_succeeded"
    - "mark_failed"
    - "GET /generations/{job_id}/audio"
    - "controlled playback URL from job storage"
  key_links:
    - "POST /generate -> rights gate -> queued job -> `BackgroundTasks` -> `process_generation_job(job_id, job_service=...)`."
    - "Queued job -> `mark_running` -> `CosyVoiceTTSProvider.synthesize(...)` -> normalized `SpeechArtifact` -> `mark_succeeded` -> local audio file."
    - "Provider exception -> `mark_failed` -> job remains visible with error metadata and no playback URL."
---

<objective>
**As a** studio operator, **I want to** hand queued jobs to the real CosyVoice runtime in-process, **so that** generation becomes a real audio-producing backend path and failures are recorded instead of stalling.

Purpose: close the missing runtime handoff while keeping the API contract queued-first and rights-gated.
Output: a lazy-loaded generation runtime, background-task route handoff, and direct success/failure regression coverage.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consented-studio-generation/02-CONTEXT.md
@.planning/phases/02-consented-studio-generation/02-RESEARCH.md
@.planning/phases/02-consented-studio-generation/02-PATTERNS.md
@.planning/phases/02-consented-studio-generation/02-VERIFICATION.md
@.planning/phases/02-consented-studio-generation/02-03-SUMMARY.md
@.planning/phases/02-consented-studio-generation/02-04-SUMMARY.md
@services/api/app/routes/generate.py
@services/api/app/services/generation_jobs.py
@services/speech-worker/providers/cosyvoice_provider.py
@services/api/tests/test_generate_stub.py
@services/api/tests/test_generation_jobs.py
@services/api/tests/test_rights_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing runtime tests and isolate route tests from live background execution</name>
  <files>services/api/tests/conftest.py, services/api/tests/test_generation_runtime.py</files>
  <read_first>
    services/api/app/routes/generate.py
    services/api/app/services/generation_jobs.py
    services/speech-worker/providers/cosyvoice_provider.py
    services/api/tests/test_generate_stub.py
    services/api/tests/test_generation_jobs.py
    services/api/tests/test_rights_gate.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
    .planning/phases/02-consented-studio-generation/02-VERIFICATION.md
  </read_first>
  <behavior>
    - A direct call to `process_generation_job(job_id, job_service=fake_service, provider=fake_provider)` transitions a queued job through running to succeeded, stores the returned audio bytes, and preserves the controlled playback URL.
    - A provider exception transitions the job to failed and preserves the error message with no playback URL.
    - A test-only one-shot failure seam, gated by the Playwright/CI env and driven by the `playwright-fail-once` marker, can fail exactly one marked live job before provider synthesis and then clear itself so the cached retry can succeed.
    - The legacy route tests remain deterministic because the background-task callable is stubbed out in pytest.
  </behavior>
  <action>
    Create `services/api/tests/test_generation_runtime.py` as the RED contract for the new job runner, including a one-shot seam case driven by the `playwright-fail-once` marker so the backend must fail exactly one marked live job before provider synthesis and then allow the cached retry to succeed. Add an autouse fixture in `services/api/tests/conftest.py` that patches the route-level background-task callable to a no-op for the legacy queued-job tests. Keep the assertions centered on the production handoff, not the stub route.
  </action>
  <acceptance_criteria>
    `process_generation_job` tests are red until the runtime module exists, and the legacy `/generate` route tests still stay green under the no-op dispatch patch.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/api/tests/test_generation_runtime.py -q</automated>
  </verify>
  <done>
    The API now has a failing contract for the live runtime handoff and isolated route tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the real provider runtime into the generation route</name>
  <files>services/api/app/services/generation_runtime.py, services/api/app/routes/generate.py</files>
  <read_first>
    services/api/app/routes/generate.py
    services/api/app/services/generation_jobs.py
    services/speech-worker/providers/cosyvoice_provider.py
    services/api/tests/test_generation_runtime.py
    services/api/tests/test_generation_jobs.py
    services/api/tests/test_generate_stub.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
    .planning/phases/02-consented-studio-generation/02-VERIFICATION.md
  </read_first>
  <action>
    Implement `process_generation_job(job_id, *, job_service=None, provider=None)` in `services/api/app/services/generation_runtime.py` so it lazily loads `CosyVoiceTTSProvider` from the approved worker root only when the default provider is needed, marks the queued job running before synthesis, passes `record.tone_preset.value` into `synthesize(...)`, stores the returned normalized clip through `GenerationJobService.mark_succeeded(...)`, and catches provider/runtime errors to call `mark_failed(...)`. Add the one-shot Playwright failure-marker seam behind the test-only env gate so a marked job fails before synthesis, then clears the trigger and keeps the production rights/provider boundaries unchanged. Update `services/api/app/routes/generate.py` so `POST /generate` creates the queued job after `ensure_voice_allowed(...)`, then schedules `process_generation_job(...)` through `BackgroundTasks` with the current job service instance instead of blocking the response.
  </action>
  <acceptance_criteria>
    The route returns queued immediately after rights approval, the background task advances jobs to succeeded or failed, and the production path uses the real provider interface without adding a separate queue service.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/api/tests/test_generation_runtime.py services/api/tests/test_generation_jobs.py services/api/tests/test_generate_stub.py services/api/tests/test_rights_gate.py -q</automated>
  </verify>
  <done>
    The API has a real runtime handoff that produces or fails jobs without blocking the request.
  </done>
</task>

</tasks>

## Artifacts this phase produces
- `services/api/app/services/generation_runtime.py`
- `services/api/app/routes/generate.py`
- `services/api/tests/conftest.py`
- `services/api/tests/test_generation_runtime.py`
- `process_generation_job`
- `get_generation_tts_provider`
- `disable_background_generation`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API request -> background runtime | Untrusted text and tone inputs cross into the synthesis runtime after the rights gate passes. |
| API -> local object store | Generated audio bytes and playback metadata must stay path-safe and never leak raw filesystem locations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | controlled playback URL / audio route | medium | mitigate | Keep raw file paths inside `GenerationJobService` and serve only relative playback URLs from `GET /generations/{job_id}/audio`. |
| T-02-05-02 | Denial of Service | background task runtime | medium | mitigate | Catch provider/runtime exceptions, mark failed, and keep `/generate` non-blocking so synthesis cannot stall the request. |
| T-02-05-SC | Tampering | worker import seam / provider selection | high | mitigate | Lazy-load only the approved CosyVoice baseline from the worker root and fail closed if the runtime cannot resolve it. |
</threat_model>

<verification>
[Overall phase checks]
- The runtime unit test proves `process_generation_job` can succeed and fail without the real model runtime.
- The existing API route tests still pass under the no-op background-task patch.
- The phase is complete only when queued jobs hand off to the real provider path without blocking `POST /generate`.
</verification>

<success_criteria>
- The API can hand queued jobs to a background task that synthesizes a real clip through the provider interface and marks success or failure.
- The route still returns queued immediately after rights approval.
- The runtime tests prove audio storage and failure handling with fakes while route tests remain deterministic.
</success_criteria>

<output>
Create `.planning/phases/02-consented-studio-generation/02-05-SUMMARY.md` when done.
</output>
