---
phase: 02-consented-studio-generation
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - services/api/app/schemas/generation.py
  - services/api/app/services/generation_jobs.py
  - services/api/app/services/object_store.py
  - services/api/app/routes/generate.py
  - services/api/app/routes/generations.py
  - services/api/app/main.py
  - services/api/tests/test_generation_jobs.py
  - services/api/tests/conftest.py
  - apps/web/components/studio-shell.tsx
  - apps/web/app/globals.css
  - apps/web/next.config.ts
  - apps/web/tests/studio-generation.spec.ts
autonomous: true
requirements: [GOV-04, STUD-05, STUD-06, STUD-07, PIPE-03]
must_haves:
  truths:
    - "D-08/D-17/D-18/D-19/D-20 and STUD-06: generated clips are stored through a local object-store abstraction, surfaced through controlled playback URLs, and shown in the browser as the current clip plus recent in-session attempts."
    - "D-09/D-10/D-11/D-12 and STUD-05/STUD-07/PIPE-03: the job lifecycle persists queued/running/succeeded/failed, retry creates a new job from the last text/voice/tone, failed metadata stays visible, and the stored metadata includes provider, voice, tone, and timing."
    - "GOV-04: consent/license notes remain attached to backend generation metadata before any clip is used."
    - "README drift stays a docs follow-up only; this phase does not reuse the stale README stack as a source of truth for product or deployment decisions."
  artifacts:
    - "services/api/app/schemas/generation.py"
    - "services/api/app/services/generation_jobs.py"
    - "services/api/app/services/object_store.py"
    - "services/api/app/routes/generate.py"
    - "services/api/app/routes/generations.py"
    - "services/api/app/main.py"
    - "services/api/tests/test_generation_jobs.py"
    - "services/api/tests/conftest.py"
    - "apps/web/components/studio-shell.tsx"
    - "apps/web/app/globals.css"
    - "apps/web/next.config.ts"
    - "apps/web/tests/studio-generation.spec.ts"
    - "GenerationJobRecord"
    - "GenerationJobService"
    - "GenerationAttempt"
    - "LocalObjectStore"
    - "GenerationResult.playback_url"
    - "GenerationResult.audio_duration_ms"
    - "GET /generations/{job_id}"
    - "GET /generations/{job_id}/audio"
  key_links:
    - "POST /generate -> sqlite job row -> provider synthesis -> normalization -> object store -> controlled playback URL."
    - "Retry button -> cached `voice_id`/`text`/`tone_preset` -> brand new job row -> preserved failed attempts list."
    - "GenerationResult.job_id -> browser polling -> current clip card -> audio element and recent attempt history."
---

<objective>
**As a** no-login internal studio user, **I want to** enter text, choose a tone preset, generate speech through a real provider, and play the resulting audio, **so that** I can audition the consented theatrical voice loop end to end.

Purpose: Finish the studio generation loop with persisted job state, controlled playback URLs, and in-session retry behavior.
Output: A job store, local object store, browser playback UI, and end-to-end retry coverage.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consented-studio-generation/02-CONTEXT.md
@.planning/phases/02-consented-studio-generation/02-RESEARCH.md
@.planning/phases/02-consented-studio-generation/02-VALIDATION.md
@.planning/phases/02-consented-studio-generation/02-PATTERNS.md
@.planning/phases/01-no-login-vertical-skeleton/01-03-SUMMARY.md
@services/api/app/schemas/generation.py
@services/api/app/routes/generate.py
@services/api/app/services/stub_generation.py
@services/api/tests/test_generate_stub.py
@services/speech-worker/providers/contracts.py
@apps/web/components/studio-shell.tsx
@apps/web/tests/studio-generation.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing API tests for job lifecycle, retry, and controlled playback</name>
  <files>services/api/tests/test_generation_jobs.py, services/api/tests/conftest.py</files>
  <read_first>
    services/api/tests/test_generate_stub.py
    services/api/tests/test_rights_gate.py
    services/api/tests/test_voice_profile.py
    services/api/app/schemas/generation.py
    services/speech-worker/providers/contracts.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-VALIDATION.md
  </read_first>
  <behavior>
    - `POST /generate` creates a queued job and later status checks can reach running, succeeded, and failed.
    - Retry submits the last text, voice, and tone as a brand new job while the failed metadata remains visible in session state.
    - A successful job exposes a controlled playback URL instead of inline bytes.
  </behavior>
  <action>
    Create `services/api/tests/test_generation_jobs.py` and any shared fixtures in `services/api/tests/conftest.py` as the failing contract for the job store, playback URL, and retry behavior. Keep the assertions centered on the job lifecycle from D-09/D-10/D-11/D-12 and the metadata surface from PIPE-03, and verify that the blocked rights path from Phase 1 still fires before any generation work.
  </action>
  <acceptance_criteria>
    The API test suite now describes queued/running/succeeded/failed jobs, retry via resubmitted text/voice/tone, and a controlled playback URL, and it fails until the implementation task lands.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/api/tests/test_generation_jobs.py -q</automated>
  </verify>
  <done>
    The API has an executable contract for job state, retry, and playback URL behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the sqlite-backed generation job service and object store</name>
  <files>services/api/app/schemas/generation.py, services/api/app/services/generation_jobs.py, services/api/app/services/object_store.py, services/api/app/routes/generate.py, services/api/app/routes/generations.py, services/api/app/main.py</files>
  <read_first>
    services/api/app/schemas/generation.py
    services/api/app/routes/generate.py
    services/api/app/services/stub_generation.py
    services/speech-worker/providers/contracts.py
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-RESEARCH.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
  </read_first>
  <behavior>
    - `POST /generate` creates a new job row and returns a queued prototype response.
    - `GET /generations/{job_id}` returns status and metadata, and `GET /generations/{job_id}/audio` returns a controlled file response.
    - Failed metadata is preserved, and every retry creates a new job row rather than mutating the old one.
  </behavior>
  <action>
    Replace the remaining stub path with a real `generation_jobs.py` service backed by sqlite3 plus a filesystem-based `LocalObjectStore`. Keep `ensure_voice_allowed` at the top of every generation entrypoint, persist provider/voice/tone/timing metadata per PIPE-03, and attach `playback_url` only after the clip has been normalized and written under the local storage directory. Add `services/api/app/routes/generations.py` for the job-status and audio-serving endpoints and include it in `services/api/app/main.py`. Do not leak raw filesystem paths or inline audio bytes.
  </action>
  <acceptance_criteria>
    The API can create a queued job, update it through running to succeeded or failed, serve the normalized clip through a controlled URL, and preserve the failed row for retry and audit visibility.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest services/api/tests/test_generation_jobs.py services/api/tests/test_generate_stub.py services/api/tests/test_rights_gate.py services/api/tests/test_voice_profile.py -q</automated>
  </verify>
  <done>
    The backend now persists generation jobs and serves playback through the local object-store abstraction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the studio shell to playback, recent attempts, and retry</name>
  <files>apps/web/components/studio-shell.tsx, apps/web/app/globals.css, apps/web/next.config.ts, apps/web/tests/studio-generation.spec.ts</files>
  <read_first>
    apps/web/components/studio-shell.tsx
    apps/web/app/globals.css
    apps/web/next.config.ts
    apps/web/tests/studio-generation.spec.ts
    .planning/phases/02-consented-studio-generation/02-CONTEXT.md
    .planning/phases/02-consented-studio-generation/02-PATTERNS.md
  </read_first>
  <behavior>
    - The current clip is prominent and playable from a controlled URL.
    - Recent attempts stay visible for the current browser session only.
    - Retry reuses the last text, voice, and tone preset and appends a new attempt card.
  </behavior>
  <action>
    Upgrade `StudioShell` into the full phase-2 studio surface per D-17/D-18/D-20: keep the text field and tone presets from 02-01, poll the generation-job routes until the latest attempt succeeds or fails, render the current clip with browser audio controls, keep the previous attempts visible with their status/provider/tone/timing metadata, and make the retry button resubmit the last `voice_id`/`text`/`tone_preset` as a brand new job. Update `apps/web/next.config.ts` for the new generations/status/audio routes and revise the Playwright spec so it proves playback and retry behavior end to end.
  </action>
  <acceptance_criteria>
    The studio can play the generated clip, shows the current clip plus recent attempts, and retries by resubmitting the last generation inputs without introducing durable history or refresh restoration.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec playwright test tests/studio-generation.spec.ts</automated>
  </verify>
  <done>
    The root studio now closes the loop from text entry to playable audio and retry.
  </done>
</task>

</tasks>

## Artifacts this phase produces
- `GenerationJobRecord`
- `GenerationJobService`
- `GenerationAttempt`
- `LocalObjectStore`
- `GenerationResult.playback_url`
- `GenerationResult.audio_duration_ms`
- `GET /generations/{job_id}`
- `GET /generations/{job_id}/audio`
- `services/api/app/schemas/generation.py`
- `services/api/app/services/generation_jobs.py`
- `services/api/app/services/object_store.py`
- `services/api/app/routes/generate.py`
- `services/api/app/routes/generations.py`
- `services/api/app/main.py`
- `services/api/tests/test_generation_jobs.py`
- `services/api/tests/conftest.py`
- `apps/web/components/studio-shell.tsx`
- `apps/web/app/globals.css`
- `apps/web/next.config.ts`
- `apps/web/tests/studio-generation.spec.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → job-status API | The client sees job state and playback URLs but cannot decide the underlying generation state. |
| API → sqlite/object store | Generation metadata and audio artifacts cross into local persistence and must stay path-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | `LocalObjectStore` / playback route | medium | mitigate | Serve audio only through controlled URLs or `FileResponse`; do not expose raw filesystem paths or inline bytes. |
| T-02-03-02 | Tampering / DoS | job store and retry flow | medium | mitigate | Keep job rows immutable once written, create a new job for every retry, and return status through a bounded polling path. |
| T-02-03-SC | Tampering | local sqlite and object-store writes | high | mitigate | Constrain all persistence to the local storage root and block any code path that tries to bypass the object-store abstraction. |
</threat_model>

<verification>
[Overall phase checks]
- The API job tests and the browser playback/retry test both pass with the local sqlite/object-store setup.
- The phase is complete only when a generated clip is playable from a controlled URL and the previous attempts remain session-scoped.
</verification>

<success_criteria>
- The studio shows the current playable clip prominently and keeps recent attempts visible for the session.
- The backend stores provider, voice, tone, and timing metadata with each job.
- Retry creates a new job from the cached last inputs and preserves the failed job metadata.
- Generated audio is served from a controlled URL through the local object-store abstraction, not as inline bytes.
- The README remains a docs-drift follow-up, not a source of truth for scope or infrastructure decisions.
</success_criteria>

<output>
Create `.planning/phases/02-consented-studio-generation/02-03-SUMMARY.md` when done.
</output>
