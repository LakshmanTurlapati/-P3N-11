---
phase: 01-no-login-vertical-skeleton
plan: 01-01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/components/studio-shell.tsx
  - apps/web/lib/voice-registry.ts
  - apps/web/playwright.config.ts
  - apps/web/tests/root-route.spec.ts
autonomous: false
requirements:
  - GOV-03
  - STUD-01
  - STUD-02
must_haves:
  truths:
    - "User can open `/` and land directly in the studio without a login or landing detour."
    - "User can see Vesper Glass as an original theatrical voice profile with a visible boundary note."
    - "Phase 2 controls are absent from the first studio surface."
  artifacts:
    - path: "pyproject.toml"
      provides: "Root pytest configuration for later Python checks"
      contains: "tool.pytest.ini_options"
    - path: "apps/web/package.json"
      provides: "Web app scripts and dependencies"
      exports:
        - "dev"
        - "build"
        - "test"
    - path: "apps/web/tsconfig.json"
      provides: "TypeScript project configuration for the web app"
    - path: "apps/web/app/layout.tsx"
      provides: "Root HTML wrapper"
    - path: "apps/web/app/page.tsx"
      provides: "Direct root route"
    - path: "apps/web/components/studio-shell.tsx"
      provides: "Studio shell with voice-first controls"
    - path: "apps/web/lib/voice-registry.ts"
      provides: "Render-only Vesper Glass display seed and boundary copy"
    - path: "apps/web/playwright.config.ts"
      provides: "Browser smoke test runner"
    - path: "apps/web/tests/root-route.spec.ts"
      provides: "Root-route studio smoke test"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/studio-shell.tsx"
      via: "Root page renders the studio component directly"
      pattern: "StudioShell"
    - from: "apps/web/components/studio-shell.tsx"
      to: "apps/web/lib/voice-registry.ts"
      via: "Studio reads render-only Vesper Glass display copy while the canonical rights record stays server-owned in Phase 1 backend work"
      pattern: "Vesper Glass display seed"
    - from: "apps/web/tests/root-route.spec.ts"
      to: "apps/web/app/page.tsx"
      via: "Playwright checks `/` renders studio-first and omits Phase 2 controls"
      pattern: "root route|text input|tone preset|playback"
---

## Phase Goal

**As a** no-login internal user, **I want to** open the studio directly at `/` and see Vesper Glass framed as an original theatrical voice, **so that** I can start validating the product surface without a landing detour.

<objective>
Create the studio shell and direct root route for the no-login surface.

Purpose: Establish the first user-visible entry point and keep Phase 2 controls out of the first screen.
Output: Root app shell, voice registry fixture, and a browser smoke test for the studio route.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
@.planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
@.planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
@.planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve the dependency version lines and Phase 1 Python toolchain before scaffold install</name>
  <files>
    - pyproject.toml
    - apps/web/package.json
    - apps/web/tsconfig.json
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
  </read_first>
  <action>Review the current stable major/minor lines for the web scaffold and test harness before any install work lands. Confirm `uv` as the Phase 1 Python toolchain from the stack research, and if `uv` is unavailable in the environment approve `python -m venv` + `pip` as the only fallback for the same scaffold path. Approve the chosen lines for the studio app, browser smoke tests, and the Python test config only if they stay within the researched ranges and keep the install story on one toolchain.</action>
  <acceptance_criteria>The version-line set is explicitly approved, the Phase 1 Python toolchain is explicit (`uv`, with `python -m venv` + `pip` only as fallback if `uv` is unavailable), and the executor can proceed without guessing at dependency versions or install tooling.</acceptance_criteria>
  <verify>
    <human-check>Approve or reject the version-line set and Python toolchain decision in the checkpoint response.</human-check>
  </verify>
  <done>The package and harness version decision is explicit and the scaffold may proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the direct-root studio shell</name>
  <files>
    - pyproject.toml
    - apps/web/package.json
    - apps/web/tsconfig.json
    - apps/web/app/layout.tsx
    - apps/web/app/page.tsx
    - apps/web/components/studio-shell.tsx
    - apps/web/lib/voice-registry.ts
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
    - pyproject.toml
    - apps/web/package.json
    - apps/web/tsconfig.json
  </read_first>
  <behavior>
    - `GET /` renders the studio directly, not a landing page or login gate.
    - The visible control set is voice selector, approval badge, voice profile card, and a single stub-generation button.
    - Vesper Glass is described as an original theatrical voice with a clear non-impersonation boundary.
    - Phase 2 controls such as text input, tone presets, playback, retry, and mic capture are absent.
  </behavior>
  <action>Create the root app shell and the local Vesper Glass display seed. Use `apps/web/app/layout.tsx` and `apps/web/app/page.tsx` to render `StudioShell` at `/`, keep the styling restrained and theatrical, and store render-only Vesper Glass copy in `apps/web/lib/voice-registry.ts` for the visible card, badge, and boundary note. Keep `pyproject.toml` aligned to the Phase 1 `uv` toolchain validated in Wave 0, and if `uv` is unavailable use `python -m venv` + `pip` consistently rather than splitting the backend scaffold across two package managers. Keep the browser file out of canonical rights ownership so the server-owned profile record can land in Phase 1 backend work, and do not add any Phase 2 controls.</action>
  <acceptance_criteria>The root route opens the studio directly, Vesper Glass appears as an original theatrical profile, the approval badge and profile card are visible, and no text input, tone preset, playback, retry, or microphone controls are rendered.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web build</automated>
  </verify>
  <done>The no-login studio shell is present, the browser display seed is render-only, and the app passes a production build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the browser smoke test harness</name>
  <files>
    - apps/web/playwright.config.ts
    - apps/web/tests/root-route.spec.ts
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
    - apps/web/app/layout.tsx
    - apps/web/app/page.tsx
    - apps/web/components/studio-shell.tsx
    - apps/web/lib/voice-registry.ts
    - apps/web/playwright.config.ts
  </read_first>
  <behavior>
    - Playwright can run the root-route smoke test headlessly.
    - The test fails if `/` stops rendering the studio surface or if Phase 2 controls appear.
    - The test succeeds when Vesper Glass, the approval badge, and the voice-first controls are visible.
  </behavior>
  <action>Add `apps/web/playwright.config.ts` and `apps/web/tests/root-route.spec.ts` so the browser smoke test asserts the direct-root studio, the Vesper Glass profile card, the approval badge, and the absence of text input, tone presets, playback, retry, and mic controls. Keep the test focused on the studio surface rather than internal implementation details.</action>
  <acceptance_criteria>The browser smoke test runs against the web app and catches any regression that reintroduces a landing detour or Phase 2 controls.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec playwright test tests/root-route.spec.ts</automated>
  </verify>
  <done>The root-route smoke harness exists and can prove the studio shell stays on the intended surface.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols / Outputs |
|---|---|
| `pyproject.toml` | `tool.pytest.ini_options` for later API and worker tests |
| `apps/web/package.json` | `dev`, `build`, `test` scripts; web dependency set |
| `apps/web/tsconfig.json` | TypeScript compiler configuration for the web app |
| `apps/web/app/layout.tsx` | Root HTML shell for the App Router |
| `apps/web/app/page.tsx` | `GET /` studio route |
| `apps/web/components/studio-shell.tsx` | `StudioShell` component |
| `apps/web/lib/voice-registry.ts` | Bundled `Vesper Glass` fixture and boundary copy |
| `apps/web/playwright.config.ts` | Playwright browser test configuration |
| `apps/web/tests/root-route.spec.ts` | Direct-root studio smoke test |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → studio shell | UI state can misstate the bundled voice or approval status if copy and fixture data drift. |
| Tooling → package registries | Scaffold install commands can introduce tampered packages or unsafe patch pins. |
| Web route → future API boundary | The UI must not become the source of truth for rights metadata once the backend exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `apps/web/components/studio-shell.tsx` | high | mitigate | Keep the bundled voice record in one local source and align the UI copy, selector, and profile card to the same fields. |
| T-01-02 | Spoofing | `apps/web/lib/voice-registry.ts` | high | mitigate | Use only original Vesper Glass boundary language and reject protected-identity wording in fixtures and tests. |
| T-01-03 | Information disclosure | `apps/web/tests/root-route.spec.ts` | low | accept | The smoke test asserts only public studio surface details; no private data exists in this phase. |
| T-01-SC | Tampering | package installs | high | mitigate | Human checkpoint before install plus review of the approved version lines, selected Python toolchain (`uv`, or `python -m venv` + `pip` fallback if unavailable), and lockfile output. |
| T-01-04 | Elevation of privilege | auth/session paths | low | accept | Authentication is out of scope for this phase because the product is explicitly no-login. |
</threat_model>

<verification>
Root route smoke test and build checks pass after the shell is scaffolded. The root page must render the studio directly and fail if any Phase 2 controls appear.
</verification>

<success_criteria>
The user can open `/` and immediately see the studio shell with Vesper Glass, a restrained theatrical surface, and no landing or login detour.
</success_criteria>

<output>
Create `.planning/phases/01-no-login-vertical-skeleton/01-01-SUMMARY.md` when done.
</output>
