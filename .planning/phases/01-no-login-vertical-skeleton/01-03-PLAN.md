---
phase: 01-no-login-vertical-skeleton
plan: 01-03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - services/api/app/schemas/generation.py
  - services/speech-worker/providers/contracts.py
  - services/api/app/services/stub_generation.py
  - services/api/app/routes/generate.py
  - services/api/app/main.py
  - services/api/tests/test_generate_stub.py
  - services/speech-worker/tests/test_provider_contracts.py
  - apps/web/components/studio-shell.tsx
  - apps/web/tests/studio-generation.spec.ts
autonomous: true
requirements:
  - PIPE-01
must_haves:
  truths:
    - "The studio can trigger the single Phase 1 generation action and receive a structured metadata result card. D-15: structured result presentation; D-16: required result fields."
    - "The result card shows provider type, voice id, rights check result, placeholder result metadata, compact provider trace, and start/end/duration timing. D-16: result payload shape; D-17: timing capture; D-19: compact secondary provider details."
    - "The studio still gets structured generation metadata on the visible card."
    - "The Phase 1 generation path returns metadata only and does not expose audio playback or real synthesis. D-18: no audio playback surface."
    - "The visible studio surface still omits text input, tone presets, retry UX, microphone, playback, and live-conversation controls."
  artifacts:
    - path: "services/api/app/schemas/generation.py"
      provides: "Generation request, result, timing, and trace models"
    - path: "services/speech-worker/providers/contracts.py"
      provides: "Typed VAD, STT, TTS, and speech-to-speech provider interfaces behind the swappable speech boundary"
    - path: "services/api/app/services/stub_generation.py"
      provides: "Metadata-only stub generation service"
    - path: "services/api/app/routes/generate.py"
      provides: "Rights-gated generation route"
    - path: "services/api/app/main.py"
      provides: "API app mounting for the generation route"
    - path: "services/api/tests/test_generate_stub.py"
      provides: "Structured stub-generation API tests"
    - path: "services/speech-worker/tests/test_provider_contracts.py"
      provides: "Provider contract shape tests"
    - path: "apps/web/components/studio-shell.tsx"
      provides: "StudioShell generation trigger and structured metadata card"
    - path: "apps/web/tests/studio-generation.spec.ts"
      provides: "Browser test for the studio trigger and result card"
  key_links:
    - from: "services/api/app/routes/generate.py"
      to: "services/api/app/services/stub_generation.py"
      via: "The route delegates the allowed path to the stub service after rights checks pass"
      pattern: "rights gate | structured metadata"
    - from: "services/api/app/services/stub_generation.py"
      to: "services/speech-worker/providers/contracts.py"
      via: "The stub service emits provider trace and timing data shaped by the provider boundary"
      pattern: "provider type | timing | trace"
    - from: "apps/web/components/studio-shell.tsx"
      to: "services/api/app/routes/generate.py"
      via: "The single studio generation action posts the selected Vesper Glass voice to the API and renders the structured metadata card"
      pattern: "trigger | structured result card"
    - from: "apps/web/tests/studio-generation.spec.ts"
      to: "apps/web/components/studio-shell.tsx"
      via: "The browser test proves the trigger/result-card loop without introducing Phase 2 controls"
      pattern: "provider type | voice id | rights check | trace | timing"
    - from: "services/api/tests/test_generate_stub.py"
      to: "services/api/app/routes/generate.py"
      via: "Tests assert the allowed and blocked responses stay metadata-only"
      pattern: "Generation blocked: this voice profile is missing approved rights metadata."
---

## Phase Goal

**As a** studio user, **I want to** click the single stub-generation action in the studio, trigger the rights-gated request, and see the structured metadata result card, **so that** the web-to-API speech boundary is proven before real audio providers are added.

<objective>
Build the speech provider interfaces, the rights-gated stub generation path, and the final studio trigger/result-card integration.

Purpose: Prove the backend speech boundary, structured metadata response, browser trigger, and timing capture before any real synthesis provider is introduced.
Output: Generation schema, provider contracts, stub generation service, generation route, studio trigger, result card rendering, and contract tests.
</objective>

<execution_context>
@/Users/akhilasusarla/.codex/gsd-core/workflows/execute-plan.md
@/Users/akhilasusarla/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
@.planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
@.planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
@.planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
@.planning/phases/01-no-login-vertical-skeleton/01-02-PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define generation contracts and provider interfaces</name>
  <files>
    - services/api/app/schemas/generation.py
    - services/speech-worker/providers/contracts.py
    - services/speech-worker/tests/test_provider_contracts.py
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-02-PLAN.md
    - services/api/app/schemas/voice_profile.py
    - services/api/app/services/rights_gate.py
  </read_first>
  <action>Define the generation request, result, timing, and provider-trace models, then add typed interfaces for VAD, STT, TTS, and speech-to-speech providers behind the worker boundary. Keep the boundary narrow and swappable per PIPE-01 so later model choices only replace adapters, not the studio contract.</action>
  <acceptance_criteria>The generation schema can describe provider type, voice id, rights check result, structured result metadata, compact provider trace, start time, end time, and duration; the provider contracts cover VAD, STT, TTS, and speech-to-speech candidates without binding the app to one model repository.</acceptance_criteria>
  <verify>
    <automated>pytest services/speech-worker/tests/test_provider_contracts.py -q</automated>
  </verify>
  <done>The generation contracts and provider interfaces are implemented and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the rights-gated stub generation route</name>
  <files>
    - services/api/app/services/stub_generation.py
    - services/api/app/routes/generate.py
    - services/api/app/main.py
    - services/api/tests/test_generate_stub.py
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-02-PLAN.md
    - services/api/app/schemas/generation.py
    - services/speech-worker/providers/contracts.py
  </read_first>
  <action>Implement the metadata-only stub generation service, the generation route, and the API app mounting. Keep the stub implementation inline in the API control plane for Phase 1; do not introduce a separately running `services/speech-worker` process yet, but preserve `services/speech-worker/providers/contracts.py` as the future extraction boundary. The route must call the server-side rights gate from Phase 1 before it assembles the response, must return structured metadata only, and must not introduce any audio payload or playback surface.</action>
  <acceptance_criteria>Approved requests return structured metadata with provider type, voice id, rights check result, result metadata, compact provider trace, and timing; blocked requests fail with the exact Phase 1 safety message `Generation blocked: this voice profile is missing approved rights metadata.`; and no audio field or playback behavior is exposed in the response.</acceptance_criteria>
  <verify>
    <automated>pytest services/api/tests/test_generate_stub.py -q</automated>
  </verify>
  <done>The rights-gated stub generation route is implemented and validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire StudioShell to the generation route and render the result card</name>
  <files>
    - apps/web/components/studio-shell.tsx
    - apps/web/tests/studio-generation.spec.ts
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-CONTEXT.md
    - .planning/phases/01-no-login-vertical-skeleton/01-RESEARCH.md
    - .planning/phases/01-no-login-vertical-skeleton/01-VALIDATION.md
    - .planning/phases/01-no-login-vertical-skeleton/01-PATTERNS.md
    - .planning/phases/01-no-login-vertical-skeleton/01-01-PLAN.md
    - .planning/phases/01-no-login-vertical-skeleton/01-02-PLAN.md
    - apps/web/tests/root-route.spec.ts
    - services/api/app/routes/generate.py
    - services/api/app/schemas/generation.py
    - services/api/app/services/stub_generation.py
    - services/api/tests/test_generate_stub.py
    - apps/web/components/studio-shell.tsx
  </read_first>
  <behavior>
    - The existing single stub-generation action in `StudioShell` sends the selected Vesper Glass voice id to the generation route and does not add new text input, tone preset, retry, mic, playback, or conversation controls.
    - A structured metadata result card appears after the request and shows provider type, voice id, rights check result, placeholder result metadata, compact provider trace, started time, ended time, and duration.
    - The card stays metadata-only; no audio payload or playback surface appears.
  </behavior>
  <action>Create the browser regression test first so the web-to-API trigger and result-card contract are explicit, then update `StudioShell` to call the generation route and render the structured metadata card from the API response. Keep the interaction inside the existing Phase 1 studio surface, reuse the single stub-generation action, and do not introduce any Phase 2 controls.</action>
  <acceptance_criteria>The studio can trigger the generation route from the existing stub-generation control, render the structured metadata card with provider type, voice id, rights check result, placeholder result metadata, compact provider trace, and timing, and still avoid text input, tone presets, playback, retry, mic, or live-conversation controls.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec playwright test tests/studio-generation.spec.ts</automated>
  </verify>
  <done>The browser can trigger the generation route from StudioShell and display the structured metadata result card without exposing Phase 2 controls or audio playback.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols / Outputs |
|---|---|
| `services/api/app/schemas/generation.py` | `GenerationRequest`, `GenerationResult`, `GenerationTiming`, `ProviderTraceEntry` |
| `services/speech-worker/providers/contracts.py` | `VADProvider`, `STTProvider`, `TTSProvider`, `SpeechToSpeechProvider` |
| `services/api/app/services/stub_generation.py` | `build_stub_generation_result` or equivalent metadata-only service |
| `services/api/app/routes/generate.py` | `router` exposing the rights-gated generation endpoint |
| `services/api/app/main.py` | `app` mounting the generation route |
| `services/api/tests/test_generate_stub.py` | generation response-shape and blocked-path tests |
| `services/speech-worker/tests/test_provider_contracts.py` | provider contract and interface tests |
| `apps/web/components/studio-shell.tsx` | `StudioShell` generation trigger and structured metadata result card |
| `apps/web/tests/studio-generation.spec.ts` | browser integration test for the trigger and result card |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> stub generation service | Request and response shapes must stay typed so stub metadata cannot drift. |
| API -> speech-worker contracts | Provider interfaces must stay swappable and must not leak provider-specific assumptions into the route. |
| Generation response -> studio UI | The stub path must not accidentally expose audio payloads or playback semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Tampering | `services/api/app/schemas/generation.py` | high | mitigate | Type the request/result models so the route cannot accept or return malformed generation metadata. |
| T-01-03-02 | Information disclosure | `services/api/app/routes/generate.py` | medium | mitigate | Return only compact trace and timing data, not raw audio or internal provider internals. |
| T-01-03-03 | Denial of service | `services/api/app/services/stub_generation.py` | medium | mitigate | Keep Phase 1 metadata-only so the allowed path cannot block on real synthesis work. |
| T-01-03-04 | Elevation of privilege | direct generation calls | high | mitigate | Reuse the server-side rights gate so bypassing the UI cannot bypass approval checks. |
</threat_model>

<verification>
Run the provider-contract, stub-generation, root-route, and studio-generation tests after the schema, service, route, and studio trigger are in place. The allowed path must emit structured metadata only, the blocked path must return the exact Phase 1 safety message, and the browser trigger must render the structured result card without exposing Phase 2 controls.
</verification>

<success_criteria>
The generation endpoint returns structured metadata only, the browser can trigger it from StudioShell and render the structured result card, the provider interfaces are in place for future VAD/STT/TTS/speech-to-speech models, and the Phase 1 stub path proves the backend boundary without audio playback.
</success_criteria>

<output>
Create `.planning/phases/01-no-login-vertical-skeleton/01-03-SUMMARY.md` when done.
</output>
